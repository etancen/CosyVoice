# CosyVoice 项目设计文档

## 1. 项目概述

CosyVoice 是一个先进的文本到语音（TTS）系统，结合了大语言模型（LLM）的能力，提供高质量、多语言、低延迟的语音合成服务。

### 1.1 主要功能与特点

- **多语言支持**：支持中文、英文、日语、韩语以及多种中国方言（粤语、四川话、上海话、天津话、武汉话等）
- **跨语言与混合语言**：支持零样本语音克隆在跨语言和代码切换场景中的应用
- **超低延迟**：集成离线和流式建模技术，实现低至150ms的首包延迟
- **高精度发音**：相比CosyVoice 1.0，发音错误率降低30%-50%
- **强稳定性**：确保零样本和跨语言语音合成的音色一致性
- **自然体验**：增强的韵律和声音质量，MOS评分从5.4提升至5.53

### 1.2 版本演进

- **CosyVoice 1.0**：基础版本，提供基本的TTS功能
- **CosyVoice 2.0**：增强版，提供更准确、更稳定、更快的语音生成能力
- **CosyVoice 3.0**：最新版本，进一步优化性能和功能

## 2. 系统架构

CosyVoice 采用模块化架构设计，主要由前端处理、语言模型、流模型和波形生成器四个核心模块组成。

### 2.1 整体架构图

```
┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐
│                 │    │                 │    │                 │    │                 │
│   前端处理模块   │───▶│    LLM模块     │───▶│    Flow模块    │───▶│   HiFT模块     │
│  (FrontEnd)     │    │ (Language Model)│    │                │    │ (Waveform Gen) │
│                 │    │                 │    │                 │    │                 │
└─────────────────┘    └─────────────────┘    └─────────────────┘    └─────────────────┘
```

### 2.2 核心组件说明

#### 2.2.1 前端处理模块（FrontEnd）

负责文本规范化、特征提取和语音处理，主要功能包括：
- 文本规范化（数字转换、标点处理等）
- 文本标记化（Tokenization）
- 语音特征提取（Mel谱图）
- 说话人嵌入提取（Speaker Embedding）
- 语音标记化（Speech Tokenization）

#### 2.2.2 语言模型模块（LLM）

将文本转换为语音标记序列，主要组件包括：
- 文本编码器（Text Encoder）
- 语言模型（Language Model）
- 采样策略（如RAS采样）

#### 2.2.3 流模型模块（Flow）

将语音标记序列转换为梅尔谱图，主要组件包括：
- 标记嵌入（Token Embedding）
- 编码器（Encoder）
- 长度调节器（Length Regulator）
- 流匹配解码器（Flow Matching Decoder）

#### 2.2.4 波形生成器模块（HiFT）

将梅尔谱图转换为波形，主要组件包括：
- 生成器（Generator）
- 源模块（Source Module）
- 声码器（Vocoder）

## 3. 核心功能模块

### 3.1 文本到语音合成（TTS）

支持多种合成模式，满足不同场景需求：

#### 3.1.1 SFT推理模式

基于预训练模型的标准语音合成，支持指定说话人ID。

```python
# 示例：SFT推理
model.inference_sft(tts_text="这是一段测试文本", spk_id="speaker001")
```

#### 3.1.2 零样本推理模式

通过参考音频和文本，实现零样本语音克隆。

```python
# 示例：零样本推理
model.inference_zero_shot(tts_text="这是一段测试文本", prompt_text="参考文本", prompt_speech_16k=audio_data)
```

#### 3.1.3 跨语言推理模式

支持使用一种语言的参考音频来合成另一种语言的语音。

```python
# 示例：跨语言推理
model.inference_cross_lingual(tts_text="This is a test text", prompt_speech_16k=chinese_audio_data)
```

#### 3.1.4 指令推理模式

通过指令控制语音风格、情感等特性。

```python
# 示例：指令推理
model.inference_instruct(tts_text="这是一段测试文本", spk_id="speaker001", instruct_text="用愉快的语气说话")
```

### 3.2 语音转换（VC）

将源说话人的语音转换为目标说话人的语音，保持内容不变。

```python
# 示例：语音转换
model.inference_vc(source_speech_16k=source_audio, prompt_speech_16k=target_audio)
```

### 3.3 流式推理

支持实时流式生成语音，实现低延迟合成。

```python
# 示例：流式推理
for chunk in model.inference_sft(tts_text="这是一段长文本", spk_id="speaker001", stream=True):
    # 处理合成的音频块
    process_audio_chunk(chunk['tts_speech'])
```

## 4. 技术栈与依赖

| 类别 | 技术/库 | 用途 |
|------|---------|------|
| 主要语言 | Python 3.10 | 核心开发语言 |
| 深度学习框架 | PyTorch | 模型训练与推理 |
| 音频处理 | Torchaudio, librosa | 音频特征提取与处理 |
| 模型优化 | TensorRT, JIT | 推理加速 |
| 大模型支持 | vLLM | 高效LLM推理 |
| API接口 | gRPC, FastAPI | 服务部署 |
| 容器化 | Docker | 环境隔离与部署 |
| 前端处理 | ttsfrd, WeTextProcessing | 文本规范化 |
| 第三方模块 | Matcha-TTS | 提供部分TTS组件 |

## 5. 关键算法与实现

### 5.1 语言模型（TransformerLM）

基于Transformer的语言模型，将文本映射到语音标记空间。核心实现包括：
- 文本编码器（ConformerEncoder）
- 自回归解码器
- 重复感知采样（RAS）策略，提高生成稳定性

### 5.2 流模型（MaskedDiffWithXvec）

基于流匹配（Flow Matching）的模型，将语音标记转换为梅尔谱图：
- 使用掩码训练策略
- 结合说话人嵌入（x-vector）控制音色
- 支持流式推理的因果版本（CausalMaskedDiffWithXvec）

### 5.3 波形生成器（HiFTGenerator）

改进的HiFiGAN生成器，结合神经源滤波（NSF）技术：
- 谐波-噪声源模型（Hn-NSF）
- 残差块设计
- 多阶段上采样

## 6. 系统优化

### 6.1 推理加速

- **JIT编译**：将模型编译为TorchScript，加速推理
- **TensorRT**：使用TensorRT优化关键组件
- **vLLM支持**：对于CosyVoice2，支持使用vLLM进行LLM部分的高效推理
- **FP16支持**：使用半精度计算加速推理

### 6.2 流式处理优化

- **KV缓存**：使用键值缓存加速自回归生成
- **动态分块**：根据RTF调整处理块大小
- **重叠处理**：使用重叠窗口避免块边界不连续

## 7. 部署方案

### 7.1 本地推理

提供Python CLI接口，支持直接在本地运行推理。

```python
from cosyvoice.cli.cosyvoice import CosyVoice2

model = CosyVoice2(model_dir="pretrained_models/CosyVoice2-0.5B", fp16=True)
for audio in model.inference_sft("这是一段测试文本", "speaker001"):
    # 处理音频输出
```

### 7.2 Web界面

提供WebUI界面，方便用户交互使用。

```bash
python webui.py --port 50000 --model_dir pretrained_models/CosyVoice-300M
```

### 7.3 服务部署

#### 7.3.1 gRPC服务

```bash
docker run -d --runtime=nvidia -p 50000:50000 cosyvoice:v1.0 /bin/bash -c "cd /opt/CosyVoice/CosyVoice/runtime/python/grpc && python3 server.py --port 50000 --max_conc 4 --model_dir iic/CosyVoice-300M && sleep infinity"
```

#### 7.3.2 FastAPI服务

```bash
docker run -d --runtime=nvidia -p 50000:50000 cosyvoice:v1.0 /bin/bash -c "cd /opt/CosyVoice/CosyVoice/runtime/python/fastapi && python3 server.py --port 50000 --model_dir iic/CosyVoice-300M && sleep infinity"
```

#### 7.3.3 TensorRT-LLM部署

使用NVIDIA TensorRT-LLM进行高性能部署。

```bash
cd runtime/triton_trtllm
docker-compose up -d
```

## 8. 配置与模型

### 8.1 配置文件

使用YAML格式的配置文件定义模型参数和结构：
- `cosyvoice.yaml`：CosyVoice 1.0模型配置
- `cosyvoice2.yaml`：CosyVoice 2.0模型配置

### 8.2 预训练模型

提供多种预训练模型：
- `CosyVoice2-0.5B`：最新的0.5B参数模型
- `CosyVoice-300M`：基础300M参数模型
- `CosyVoice-300M-SFT`：经过监督微调的模型
- `CosyVoice-300M-Instruct`：支持指令控制的模型

## 9. 使用指南

### 9.1 安装与环境配置

```bash
# 克隆仓库
git clone --recursive https://github.com/FunAudioLLM/CosyVoice.git
cd CosyVoice
git submodule update --init --recursive

# 创建conda环境
conda create -n cosyvoice -y python=3.10
conda activate cosyvoice
pip install -r requirements.txt -i https://mirrors.aliyun.com/pypi/simple/ --trusted-host=mirrors.aliyun.com
```

### 9.2 基本使用

```python
import sys
sys.path.append('third_party/Matcha-TTS')
from cosyvoice.cli.cosyvoice import CosyVoice2
from cosyvoice.utils.file_utils import load_wav
import torchaudio

# 初始化模型
model = CosyVoice2(model_dir="pretrained_models/CosyVoice2-0.5B")

# SFT推理
for audio_chunk in model.inference_sft("这是一段测试文本", "speaker001"):
    torchaudio.save("output.wav", audio_chunk['tts_speech'], 24000)
```

### 9.3 高级功能

#### 9.3.1 添加零样本说话人

```python
# 加载参考音频
prompt_speech_16k, _ = load_wav("reference.wav", 16000)

# 添加零样本说话人
model.add_zero_shot_spk("参考文本", prompt_speech_16k, "new_speaker")

# 使用新添加的说话人
for audio_chunk in model.inference_sft("这是一段测试文本", "new_speaker"):
    torchaudio.save("output.wav", audio_chunk['tts_speech'], 24000)
```

#### 9.3.2 使用流式推理

```python
for audio_chunk in model.inference_sft("这是一段较长的测试文本，用于演示流式推理功能", "speaker001", stream=True):
    # 实时处理音频块
    process_audio_chunk(audio_chunk['tts_speech'])
```

## 10. 总结与亮点回顾

CosyVoice 是一个功能强大、性能优越的TTS系统，其主要亮点包括：

- **创新性架构**：成功将大语言模型与语音合成技术结合
- **多语言支持**：覆盖多种语言和方言，满足全球化需求
- **低延迟设计**：通过流式推理和优化技术，实现超低延迟
- **高质量输出**：采用先进的声学模型和波形生成技术，提供自然流畅的语音
- **灵活部署选项**：支持本地推理、Web界面和多种服务部署方式

CosyVoice 为语音合成领域带来了新的可能性，特别适合需要高质量、多语言、低延迟语音合成的应用场景。